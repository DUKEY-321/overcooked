# 一二 v003 首个完整产出

> 历史恢复点：当前使用 v004。v004 已把蓝帽从 `Head` 拆出；发布资源名已统一为 `YierCap`。安装与配置以 `V004_OPTIONAL_HAT_REPORT.md` 为准。

状态：`HISTORICAL_WORKSPACE_OUTPUT`。以下内容记录 v003 当时的产出状态；该版本随后已归档，当前安装为 v004。

## 产物

- Blender：`F:\dev\overcooke\characters\yier\source\yier_work-v003.blend`
- Blender SHA-256：`20253EDD5DBD2D25ADA1FDDFD0CCF55B13A72232F229C1C621131A669CE5C0D0`
- 资源包：`F:\dev\overcooke\exports\Resources\174-yier`
- 角色 ID：`174`（本机现有资源仅占用 170–173）

| 部件 | 三角面 | 首版处理 |
| --- | ---: | --- |
| Head | 5,520 | 主头、蓝帽、耳朵、粉腮和嘴 |
| Eyes / Eyes2_Blinks | 330 / 330 | 闭眼按左右眼中心做 X×1.12、Z×0.18 |
| 四个手状态 | 每个 1,104 | 已对齐粉猪参考手位；同侧 Open/Grip 首版相同 |
| Body_Body | 3,312 | 身体、白色脚面与棕鞋底；由插件按高度生成双骨权重 |
| Body_Bottom | 6,519 | 领圈和前结，沿用现有粉猪资源的部件语义 |
| Body_Tail | 1,104 | 背部圆尾 |

资源包全部 OBJ 合计 21,531 三角面；运行时同一时刻不会同时显示睁/闭眼及两种手型。

## 已完成门禁

- 21 组跨材质双面壳及两只脚的部分重叠已按三视图可见材质清理，共移除 8,774 三角面。
- 前、侧、背以及睁眼/闭眼渲染通过，没有材质缺失或反向壳显露。
- 所有 OBJ 使用世界坐标、`Forward -Z / Up Y`、三角面、UV 和法线；重新导入后边界与面数一致。
- 图集为 512×512 RGBA PNG，每个面使用色块内非退化 UV 三角形。
- `Body_Body` 最大单三角高度跨度为 `0.024859`，没有跨越多个 0.08 权重档。
- 严格资源校验及本机 ID 冲突检查：`0 errors / 0 warnings`。

## 实机测试重点

1. 选人界面和大厅：确认头朝向、帽檐和角色比例。
2. 待机与眨眼：确认睁闭眼不跳位、不陷入脸内。
3. 走路、跑动、冲刺：观察两骨身体的弹性、脚底漂移和帽檐穿模。
4. 拿盘子、食材、灭火器，切菜和投掷：确认左右手没有反向，厨具位于圆爪内。
5. 背包、炮台及载具场景：确认尾巴与帽子遮挡。

## 下一轮完善条件

- 根据实机画面调整头部宽度/深度和手位，而不是在未测试前盲目缩放。
- 若拿取动作需要明显握持，再从 Open 手复制制作真正的 Grip 造型，同时保持手腕中心和边界不跳变。
- 针对身体弹簧变形修正领圈、脚与躯干分件；必要时再减面或补权重环线。
- 一二上述测试稳定后，才把成熟脚本参数化复用到布布。

手动安装时，应先备份游戏端 `prefer.txt`，复制整个 `174-yier` 目录到 `OC2DIYChef\Resources`，再增加一行 `174-yier`；不要添加 `HAT=Santa`，避免覆盖一二自带蓝帽。回滚时优先把该目录移出 Resources 并恢复备份的 `prefer.txt`。
